language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t marcosseoane/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true marcosseoane/docker-react npm run test
